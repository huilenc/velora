node_modules
frontend/.next/
frontend/out/

# Rust/Anchor build artifacts
target/

# Program keypair — controls who can upgrade the deployed program
# NEVER commit this — anyone with this file can take over the program
target/deploy/*-keypair.json

# Environment files with secrets
.env.local
.env*.local
**/.env.local
**/.env*.local

# Next.js builds
app/.next/
app/out/

# Anchor local validator files
.anchor/
test-ledger/
.vercel
